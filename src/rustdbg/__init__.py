"""rustdbg -- IDE-grade Rust debugging for coding agents.

A zero-dependency layer over rust-analyzer + lldb-dap that lets an agent set
breakpoints, run, read and change real runtime values, step, and navigate --
exposed both as the ``rdbg`` CLI (for skills) and as an MCP server.
"""

__version__ = "0.1.0"
